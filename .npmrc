# Enforce Node.js version requirements
engines-strict=true

# Set Node.js version requirement
engine-strict=true

# Optional: Set registry (uncomment if needed)
# registry=https://registry.npmjs.org/

# Optional: Save exact versions by default
# save-exact=true

# Optional: Set audit level
# audit-level=moderate